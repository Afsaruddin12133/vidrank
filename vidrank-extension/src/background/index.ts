// Verbatim port of the original background service worker (repo/src/js/background.js).
// Kept as a plain .js module (allowJs + checkJs:false in tsconfig) for an exact copy.
import './background.js'
