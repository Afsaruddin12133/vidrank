{
  "name": "vidrank-landing",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro preview",
    "build": "astro build",
    "preview": "astro preview",
    "pages:dev": "astro build && wrangler pages dev dist",
    "pages:deploy": "astro build && wrangler pages deploy dist --project-name vidrank-landing --commit-dirty=true",
    "pages:preview": "astro build && wrangler pages deploy dist --project-name vidrank-landing --branch preview --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^5.0.0",
    "firebase": "^12.18.0",
    "marked": "^18.0.10",
    "motion": "^12.0.0",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
